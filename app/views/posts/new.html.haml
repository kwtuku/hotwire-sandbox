.d-flex.align-items-center.mb-3
  = link_to posts_path do
    %i.bi.bi-arrow-left.fs-4
  %h1.mb-0.ms-2= t('general.new_resource', resource_name: Post.model_name.human)

= turbo_frame_tag dom_id(@post) do
  = render 'form', post: @post
