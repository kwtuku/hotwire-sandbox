.p-1
  = bootstrap_form_with(model: [post, reply], scope: :reply) do |f|
    = f.text_area :content, hide_label: true, placeholder: t('posts.reply_to_post'), wrapper: { class: 'mb-2' },
      class: 'resize-none', data: { controller: 'autosize' }
    = link_to t('general.cancel'), post_path(post), class: 'btn btn-light'
    = f.primary
