= render 'posts/header' do
  %h1.mb-0= t('posts.reply_to_post')

= turbo_frame_tag dom_id(@post, :new_reply) do
  .mb-3.pb-3.border-bottom= render 'users/posts/replies/form', reply: @reply, post: @post
