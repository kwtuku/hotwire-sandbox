= turbo_stream.update dom_id(@post, :like) do
  = button_to post_like_path(@post), method: :delete, class: 'btn btn-link' do
    %i.bi.bi-heart-fill.text-danger
  .ms-3
    %small{ class: class_names('visually-hidden': @post.likes_count.zero?) }= @post.likes_count
